apiVersion: schema.hana.orchestrate.cloud.sap/v1alpha1
kind: DbSchema
metadata:
  name: example-dbschema
spec:
  forProvider:
    schemaName: EXAMPLE_SCHEMA
    owner: DBADMIN
  providerConfigRef:
    name: example