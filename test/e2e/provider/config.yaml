apiVersion: hana.sap.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: example
spec:
  credentials:
    source: Secret
    connectionSecretRef:
      namespace: upbound-system
      name: secret
