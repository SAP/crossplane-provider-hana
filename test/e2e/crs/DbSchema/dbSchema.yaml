apiVersion: schema.hana.sap.crossplane.io/v1alpha1
kind: DbSchema
metadata:
  name: example-schema
spec:
  forProvider:
    schemaName: E2E_EXAMPLE_SCHEMA
    owner: DBADMIN
  providerConfigRef:
    name: example